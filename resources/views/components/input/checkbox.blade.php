<div class="form-cehckbox">
    <input type="checkbox" name="{{ $name ?? '' }}" value="{{ $value ?? '' }}" class="checkbox">
    <label for="{{ $name ?? '' }}">
        {{ $label ?? '' }}
    </label>
    <span></span>
</div>
