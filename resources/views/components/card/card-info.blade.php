<div class="card-info info-{{ $class ?? '' }}">
    <div class="info-icons">
        <i class="{{ $icons ?? '' }}"></i>
    </div>
    <div class="info-counter">
        <h2>{{ $counter ?? '' }}</h2>
        <span class="info">{{ $title ?? '' }}</span>
    </div>
</div>
